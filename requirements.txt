# 기본 의존성 (항상 필요)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
python-dotenv==1.0.0
sqlalchemy==2.0.23
aiosqlite==0.19.0

# 선택적 의존성 설치 방법:
# AI 기능: pip install -e ".[ai]"
# 스크래핑: pip install -e ".[scraping]"  
# 데이터베이스: pip install -e ".[database]"
# 백그라운드 태스크: pip install -e ".[background]"
# 보안: pip install -e ".[security]"
# 데이터 처리: pip install -e ".[data]"
# 개발: pip install -e ".[dev]"
# 모든 기능: pip install -e ".[all]"